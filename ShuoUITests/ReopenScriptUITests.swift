//
//  ReopenScriptUITests.swift
//  ShuoUITests
//
//  Created by Justin Chow on 13/07/26.
//

// PLACEHOLDER — this file contains no tests.
//
// Intended to cover reopening a saved script straight into the analysis step, bypassing
// purpose/input/loading. Blocked twice over: `FeatureHome` is an empty stub so there is no
// list to reopen from, and the reopen path itself is not built — `ScriptDraft` already
// carries `existingScriptID` for it, but nothing hydrates a draft from a saved `Script`.
// See ARCHITECTURE.md §3.1.1, §8.

import Foundation
